# test_figs

`test_figs.py` plots abundance scores per generation from a query run.

It reads an absolute Windows path to a run that is not in this repository, so it needs the
path changed to a local `output/` folder before it will do anything.
