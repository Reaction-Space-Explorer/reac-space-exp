# What the scripts in this folder actually import, at the versions used for the
# Chem. Sci. 2022 work. requirements-frozen-2020.txt is the original pip freeze,
# kept as the record of the whole environment.
matplotlib==3.2.1
networkx==2.4
numpy==1.15.4
pandas==0.24.2
py2neo==4.3.0
