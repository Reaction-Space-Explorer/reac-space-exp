# What the scripts in this folder actually import, at the versions used for the
# Chem. Sci. 2022 work. frozen-environment-2020.txt is the original pip freeze,
# kept as the record of the whole environment.
#
# These versions are a record of what the published work ran on, not a recommendation.
# Dependabot is switched off for this repository: it is archival research code that
# reads local files, not a deployed service, and the alerts it raised were all for
# packages the code never imported. If you are running these scripts today, install
# current versions instead; the API surface used here is small and stable.
matplotlib==3.2.1
networkx==2.4
numpy==1.15.4
pandas==0.24.2
py2neo==4.3.0
