# archive

Earlier versions of the pattern-match query, kept for reference. Copy the text of one into
the Neo4j browser to run it.

| | |
|---|---|
| `_FINAL_QUERY_single_label.TXT`, `_FINAL_QUERY_PARAMETERIZED_single_label_graph.txt` | The query against a form of the network where molecules and reactions carry one label rather than two |
| `Pereto_simple_loop.txt`, `Pereto_simple_loop (2).txt` | Matches the simple loop of Peretó, a cycle that regenerates one copy rather than more |
| `checkpoint.txt` … `checkpoint3.txt` | Working versions saved while the query was being built |
| `queries_tested_using_mock_data/` | The versions run against [`../../mock_data`](../../mock_data) |

`README.txt` is the original note and says the same.
